var React = require('react');

var Footer = React.createClass({
        render: function () {
            return (
                <div className="footer">
                    <p>Machine learning illustration: movie recommendations according to previous ratings.</p>
                </div>
            );
        }
    }
);

module.exports = Footer;